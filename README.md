## TODO: 

add CalculatorTests target and associated viewModel tests

format/truncate header rate

exchangeInputViewModel shouldn't hardcode ars, listen to currencySelectionEvent and set property within observation, replace hardcoded ars with property
